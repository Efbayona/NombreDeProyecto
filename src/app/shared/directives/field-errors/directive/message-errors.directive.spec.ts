import { MessageErrorsDirective } from './message-errors.directive';

describe('MessageErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
