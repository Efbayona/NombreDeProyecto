import { FormatValueDirective } from './format-value.directive';

describe('FormatValueDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatValueDirective();
    expect(directive).toBeTruthy();
  });
});
